# Development

## Common Commands

```bash
make fmt
make lint
make type
make test
make docs
```

## Release Flow

Use:

```bash
make release
```

This updates version metadata, generates a changelog, and prepares a tagged release.
