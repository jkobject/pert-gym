# Configuration

Default configuration files live in `config/`.

## Base Config

`config/base.yml` is the starter configuration.

You can add environment-specific files such as:

- `config/dev.yml`
- `config/prod.yml`

## Running with a Config

```bash
pert-gym run --config config/base.yml
```
