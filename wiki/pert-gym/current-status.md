# Current harmonization status

_Last updated: 2026-06-22 18:07 CEST_

## Live branch

- Lamin instance: `laminlabs/pertdata`
- Working branch: `jkobject`
- Connection rule: always use `tools.lamin_context.connect_pertdata()`; do not use the global Lamin CLI.

## Wiki navigation

- Use this page for the canonical latest status, validation evidence, and dated handoffs.
- Use [dataset-status-catalogue.md](dataset-status-catalogue.md) for detailed source/family descriptions, PRISM/T-cell blockers, temporal catalogue notes, and artifact pointers.
- Use [model-roadmap.md](model-roadmap.md) for model-ready criteria, benchmark status, and environment decisions.
- Use [lamin-audit-and-branch-model.md](lamin-audit-and-branch-model.md) for count vocabulary and branch/audit interpretation.

## P3R final verdict — real P3 complete

The original P3 metadata-only gate has been superseded by the real-P3 replacement graph. Under Jérémie's corrected definition, P3 can now honestly be considered complete for the current canonical triplet/query surface:

- public/base pertdata examples and pert-gym additions are represented together in a dated unified `ln.Collection` family;
- the canonical query surface is `pert-gym/canonical/20260621`;
- members are canonical `obs.parquet` artifacts, with `X.h5ad` and `var.parquet` resolved through `obs -> X -> var` Lamin feature links;
- query helpers and validation scripts pass for public and pert-gym-added examples;
- the exploration notebook/smoke script runs and demonstrates safe public-small-X loading plus metadata-only handling for large PRISM/VIP members.

This is not the same as saying every member is model-ready or biologically fully curated. It means the unified collection/query/notebook contract exists and is validated at the triplet-integrity level. A first reviewed `model-ready` v0 subset now exists separately with 1 tiny loader-smoked member; the remaining 1055 canonical members should still be treated as triplet-integrity/query-surface members, not model-ready training data.

## Unified Collection records

Generated by `artifacts/schema_audit/build_unified_collection_20260621.py` and recorded in:

```text
artifacts/schema_audit/unified_collection_build_20260621.json
artifacts/schema_audit/unified_collection_build_20260621.md
artifacts/schema_audit/unified_collection_manifest_20260621.tsv
```

| role | key | uid | members | status |
| --- | --- | --- | ---: | --- |
| base public | `pert-gym/base-public/20260621` | `SM2AjpxEQZ1pEfF30000` | 60 | created/read-back OK |
| pert-gym additions | `pert-gym/additions/20260621` | `kYkBznC2fuGmbUbg0000` | 996 | created/read-back OK |
| canonical union | `pert-gym/canonical/20260621` | `aEWBxMlcWx2d7Cd80000` | 1056 | created/read-back OK |
| model-ready | `pert-gym/model-ready/20260621` | `Wh15NEPlEfF86XaO0000` | 1 | created/read-back OK; first v0 loader-smoked subset |

No inherited public/main collection was mutated; the P3R build created dated branch-local `pert-gym/*` records.

## Exact count vocabulary

Do not collapse these into a single "dataset count":

| category | count | meaning |
| --- | ---: | --- |
| latest visible Lamin artifacts | 3407 | latest artifact records visible on branch `jkobject` at P3R-C build time |
| canonical collection members | 1056 | `obs.parquet` artifacts in `pert-gym/canonical/20260621` |
| base-public collection members | 60 | public/base obs-triplet members included in the canonical collection |
| pert-gym addition members | 996 | branch-added obs-triplet members included in the canonical collection |
| logical datasets / families | 120 | unique `dataset_id` / `logical_dataset` rows in the unified manifest |
| triplet prefixes / chunks | 1056 | canonical obs/X/var prefixes represented by the collection members |
| chunked members | 961 | manifest rows with `split == chunk` |
| non-chunk members | 95 | manifest rows with `split == none` |
| harmonization level: `triplet-integrity-ok` | 1056 | all canonical members in the current unified manifest |
| model-ready members | 1 | reviewed v0 loader-smoke subset; currently `viperturb/vimentin_screen_chunk_smoke/chunk_0000/obs.parquet` only |

Important correction: historical `110`/`111` counts were logical dataset/audit-subset counts, not the database size. Historical `720`/`721` counts were triplet-prefix counts from the metadata-only audit, not the database size. Use artifact, collection-member, logical-family, triplet-prefix, and model-ready counts separately.

## P7S scoped final audit — 2026-06-22

The replacement final audit requested after stale-card cleanup is recorded in:

```text
artifacts/schema_audit/final_audit_p7s_20260622.md
artifacts/schema_audit/final_audit_p7s_lightweight_evidence_20260622.json
```

Scope and verdict: P7S supersedes the stale dependency problem in old P7-Mac
`t_b0e396a4` for status-reporting purposes, while explicitly leaving unresolved
non-stale blockers deferred rather than complete. No Lamin writes, broad Lamin
queries, or full `X.h5ad`/huge-matrix loads were performed by the audit.

Current high-level state:

- P3R remains complete at the triplet-integrity/query-surface level with the
  count vocabulary above.
- P6A T-cell GWPS `D4_Rest.assigned_guide` is final-verified: 539 chunks,
  2,693,903 obs, 18,130 vars, and 1,617 latest obs/X/var artifact records;
  final verification did not load X payloads.
- P6B/XAtlas-Orion remains resource-deferred: staged raw h5ads are ~195 GiB and
  ~326 GiB, no canonical triplets exist yet, and the current Mac scratch budget
  is not sufficient for a safe local smoke.
- GSE173650/T5 is no longer an active ingestion blocker; `t_48dc431c` was
  completed as stale/excluded by user decision and should not be revisited unless
  a new explicit rat-kidney source is provided.
- Active deferred blockers include SCP authenticated-download 401 rows,
  PerturBase T29 row114 biological label/QC ambiguity plus row113
  encoded-barcode/ORF-to-TF-symbol and filtered-cell contract, XAtlas/Orion
  resources, PRISM/GDSC compound-structure/expression joins for response-screen
  modeling, and PRISM residual Drive permission/quota rows. T29 row115/GSE142078
  is accepted+verified and is not a current deferred blocker. Late 2026-06-22
  SCP browser-auth update: T-SCPAUTH-Mac (`t_a469ca1d`) attempted the logged-in
  Chrome/OMX path but Chrome control failed before page access with a local
  `node_repl/js` sandbox cwd bootstrap error. No SCP files were downloaded or
  staged. Prior headless HTTP 401 remains retryable after the OMX/Chrome tooling
  issue is fixed; evidence is in
  `artifacts/schema_audit/scp_browser_auth_recovery_20260622.{md,json}`.
- M0-M7 and MB0-MB6 are done where applicable, but the model benchmark evidence
  is mostly environment/loader/synthetic smoke. chemCPA now has one real tiny
  molecular DRUG-seq expression-response loader smoke; PRISM/GDSC remain response
  screens rather than expression-conditioned chemCPA inputs. trVAE is correctly
  blocked from legacy real benchmarks. GEARS now has an isolated official
  `cell-gears==0.1.2` dependency/API plus synthetic data-contract smoke
  (`artifacts/model_benchmarks/gears_20260622.md`) and a first tiny real
  GEARS-contract Datlinger17 CROP-seq export/smoke: 240 cells × 128 genes, one
  T-cell/Jurkat context, 40 controls, 40 cells each for REL/EGR1/FOS/NFKB1/NFATC1,
  NFKB1/NFATC1 held out, `obs.condition`/`obs.cell_type`/`obs.is_control` and
  `var.gene_name` validated (`artifacts/model_benchmarks/gears_datlinger17_tiny_20260622/report.md`,
  `artifacts/model_benchmarks/gears_datlinger17_real_smoke_20260622.md`). This is
  a real bounded adapter/API smoke, not upstream GEARS GNN training and not a
  biological performance claim. scGEN has both the original isolated
  `.venv-models/scgen` synthetic AnnData condition/control adapter smoke
  (`scgen_20260622.*`) and a bounded real VIPerturb adapter smoke
  (`scgen_real_viperturb_tiny_20260622.{json,h5ad}` plus
  `scgen_real_20260622.{json,md}`): 22 obs × 96 genes from the reviewed
  model-ready VIPerturb member, 12 controls, five CRISPRi perturbation identities,
  disjoint non-control train/val/test identities, and no synthetic fallback.
  Upstream `scgen==2.1.0` remains assessed but not enabled because current Python
  3.11 scverse resolver combinations failed import, so the real scGEN artifact is
  still an adapter smoke rather than a biological performance claim. scPRAM now
  has a real adapter semantic smoke (`artifacts/model_benchmarks/scpram_real_adapter_20260622.md`)
  that uses the upstream control/stimulated, held-out-cell-type contract; current
  model-ready v0 remains infeasible for biological scPRAM benchmarks.

## Canonical member distribution

By source in `artifacts/schema_audit/unified_collection_manifest_20260621.tsv`:

| source | members |
| --- | ---: |
| PRISM | 925 |
| VIPerturbSeq | 55 |
| scPerturb | 33 |
| Tahoe-100M | 14 |
| Cellarity | 10 |
| Arc VCC | 6 |
| Sanger | 4 |
| LINCS | 3 |
| Broad PRISM Repurposing | 1 |
| DRUG-seq | 1 |
| DepMap/CCLE | 1 |
| Ginkgo VCPI | 1 |
| SchiebingerLander2019 | 1 |
| papalexi_eccite_merged | 1 |

By modality:

| modality | members |
| --- | ---: |
| scRNA-seq | 1043 |
| screen | 6 |
| L1000 | 3 |
| unknown | 3 |
| bulk_RNA | 1 |

## Temporal pretraining catalogue v0 plan

A read-only validation/prioritization pass for `data/temporal_pretraining_datasets/temporal_pretraining_datasets_v4.*` was completed on 2026-06-21. It produced:

```text
artifacts/schema_audit/temporal_pretraining_dataset_plan_20260621.md
artifacts/schema_audit/temporal_pretraining_dataset_plan_20260621.json
```

Summary: TSV/JSON/Markdown all carry 150 data rows; TSV and JSON field sets/order match with 0 row mismatches. Recommended v0 primary temporal pretraining candidates are human embryonic limb CELLxGENE, Zebrahub, mouse gastrulation/early organogenesis, rat kidney postnatal, and reproducible brain organoids. scNT-seq GSE141851 was subsequently repaired/finished by T22R; remaining SCP regeneration and scSLAM-style rows still need source/semantics cleanup before ingestion. Schiebinger/Optimal-transport-style reprogramming should be duplicate-reviewed before any ingestion because `SchiebingerLander2019` triplets already exist in Lamin.

## Temporal pretraining batched roadmap — 2026-06-22

The one-card-per-dataset temporal plan has been replaced by a batched roadmap:

```text
artifacts/schema_audit/temporal_ingestion_batches_20260622.md
artifacts/schema_audit/temporal_ingestion_batches_20260622.json
```

The roadmap covers all 84 `A_explicit` rows from the validated 150-row catalogue, marks rows already ingested/smoked/accepted separately, and adds a small high-value `B_candidate` CELLxGENE continuation set. It defines 31 practical continuation batches grouped by source/tooling pattern — SCP session-aware downloads, BioStudies/ArrayExpress converters, GEO/RAW archive repair, OrganoidDB accession resolution, PerturBase duplicate-aware ingestion, STOmics/spatial staging, plant spatial breadth, and non-expression representation design. Most batches contain 2–3 catalogue rows; the only single-row exceptions are genuinely special large/representation tasks.

Future temporal Kanban cards should be created from these batch sections and should point back to the markdown/JSON artifacts. Do not create one follow-up card per catalogue row.

## Temporal T22R scNT-seq GSE141851 repair/finish — 2026-06-22

T22R-Mac (`t_d276238d`) resumed the interrupted scNT-seq partial state after workspace consolidation and completed the explicit GSE141851 timecourse subset. It connected through `tools.lamin_context.connect_pertdata()` to `laminlabs/pertdata` branch `jkobject`, then used metadata/obs-var-link-first verification rather than loading full matrices.

Verified result: 19 same-prefix triplets under:

```text
temporal_pretraining/metabolic_labeling/scnt_seq/GSE141851/
```

Total rows: 56,500 obs across five cortical KCl samples (`0/15/30/60/120 min`) and two mESC chase replicates (`0/0.5/1/3/6/12/24 h`). The canonical `X.h5ad` is C_new + T_old total RNA counts; linked auxiliary `X_new_counts.h5ad` and `X_old_counts.h5ad` preserve new/pre-existing RNA semantics, and every `X*` artifact links to same-prefix `var.parquet`. Nine already-written prefixes were skipped and metadata-verified; ten remaining prefixes were written and verified. Independent post-check confirmed 19/19 prefixes with payload existence, `obs -> X/X_new_counts/X_old_counts`, `X* -> var`, obs row counts, X n_observations metadata, and var row counts. No NASC work was repeated.

Artifacts:

```text
artifacts/scripts/ingest_temporal_scnt_seq_gse141851_20260622.py
artifacts/schema_audit/temporal_scnt_seq_gse141851_t22r_20260622.json
artifacts/schema_audit/temporal_scnt_seq_gse141851_t22r_20260622.md
```

Current decision: scSLAM-seq GSE115612 remains blocked. The processed 96-cell TSV exposes condition/replicate SLAM metrics, but still lacks a clean explicit multi-timepoint/labeling-window semantics path for canonical temporal ingestion.

## Temporal T15 SCP developmental neural/eye/heart batch — 2026-06-22

T15-Mac (`t_625ca1ff`) performed source/duplicate probes for rows 65 (`SCP1290`), 75 (`SCP3301`), and 79 (`SCP1467`). SCP public APIs expose file manifests for all three, but representative anonymous `download`/`stream` probes returned HTTP 401, so direct SCP ingestion still requires an authenticated browser/session-aware export.

Bounded alternate ingestion completed for row 65: matched GEO scRNA series `GSE153162` exposes a 447 MB RAW tar containing 20 per-sample 10x H5 matrices. These were ingested as one same-prefix triplet per sample under:

```text
temporal_pretraining/scp1290_gse153162_mammalian_cerebral_cortex/
```

Verification: 20 triplet prefixes, 128,746 total obs rows, 27,998 var rows per sample, stages E10 through P4 including Fezf2 het/KO contexts; payload existence and `obs -> X -> var` feature links verified for every prefix.

Artifacts:

```text
artifacts/scripts/ingest_temporal_t15_scp_gse153162_20260622.py
artifacts/schema_audit/temporal_t15_scp_gse153162_status_20260622.md
artifacts/schema_audit/temporal_t15_scp_gse153162_status_20260622.json
```

Current decision: row 75 (`SCP3301` / matched `GSE315712`) is source-resolved but not ingested because SCP payloads were auth-gated in headless probes and GEO exposes a 33 GB RAW tar with a 24 GB sample archive; continue only with a staged/chunked converter if using GEO. Row 79 (`SCP1467`) remains blocked on authenticated SCP export or a real alternate expression mirror. T-SCPAUTH-Mac (`t_a469ca1d`) did not resolve either row because OMX/Chrome control failed before authenticated page access, so this is a tooling retry blocker rather than a new source verdict.

## Temporal T19 large mouse embryo/gastrulation probe — 2026-06-22

T19-Mac (`t_dcae7faa`) performed source/duplicate probes plus bounded archive inspection for rows 61, 66, and 80. No Lamin writes and no matrix loads were performed.

Artifacts:

```text
artifacts/schema_audit/temporal_t19_mouse_embryo_batch_status_20260622.md
artifacts/schema_audit/temporal_t19_mouse_embryo_batch_status_20260622.json
artifacts/schema_audit/temporal_t19_e_mtab_6967_tar_members_20260622.txt
```

Current decision:

- Row 61 (`Extended mouse organogenesis atlas`) is `blocked_source_mismatch`: the catalogue URL points to `E-MTAB-10843`, but the BioStudies record exposes only IDF/SDRF and the SDRF organism values are `Caenorhabditis elegans` / `Homo sapiens`, not mouse. Correct the source/accession before any staging or conversion.
- Row 66 (`Mouse embryogenesis systematic reconstruction` / Descartes) is `blocked_source_endpoint_unresolved`: the public site is reachable, but the probed mouse page returns HTTP 403 and static bundle scans did not reveal a usable matrix/API/download endpoint. Resolve the real data endpoint before conversion.
- Row 80 (`E-MTAB-6967` whole mouse embryos E6.5-E8.5) is `staged_converter_ready_not_ingested`: the 1.67 GB processed archive is staged at `gs://scperturb/pert-gym/staging/data/main/temporal_pretraining/E-MTAB-6967/atlas_data.tar.gz` and verified at 1,669,415,283 bytes. Streamed tar listing found `raw_counts.mtx`, `meta.csv`, `genes.tsv`, `sizefactors.tab`, `corrected_pcas.rds`, and `README.txt`; build a chunked MatrixMarket converter and smoke one chunk before any Lamin ingestion.
- Exact Lamin duplicate searches found 0 hits for `E-MTAB-10843`, `E-MTAB-6967`, Descartes, and planned T19 prefix terms.

## Temporal T13F/T13G mouse gastrulation streaming ingestion — 2026-06-22

T13F-Mac (`t_6ca5cc4f`) implemented the streaming converter for the HCA mouse
gastrulation `atlas/raw_counts.mtx` member. The converter reads the gzipped tar
member line-by-line via `tarfile.extractfile()`, does not call `scipy.io.mmread`,
and does not extract the 6.73 GB MatrixMarket file to disk. It first verified a
bounded smoke triplet:

```text
temporal_pretraining/mouse_gastrulation_hca/chunk_00000_00099   100 × 29,452   344,772 nnz   E6.5
```

T13G-Mac (`t_b70afb5d`) then completed the full sequential production ingestion
on `laminlabs/pertdata` branch `jkobject` using a one-pass streaming chunker and
single Lamin writer. The complete dataset is now represented under:

```text
temporal_pretraining/mouse_gastrulation_hca/
```

Final verified coverage:

```text
141 same-prefix triplet prefixes
139,331 total obs rows
29,452 var rows per prefix
483,512,215 MatrixMarket entries streamed
0 column-order violations
chunk range: chunk_00000_00099 through chunk_139100_139330
final tail: chunk_139100_139330   231 × 29,452   793,336 nnz
```

Every chunk has same-prefix `obs.parquet`, `X.h5ad`, and `var.parquet` artifacts
with payload existence and `obs -> X -> var` links verified. The final read-back
verifier found 141 prefixes, 139,331 obs rows, 29,452 var rows per prefix, and no
errors. The staged raw archive remains durable on GCS at
`gs://scperturb/pert-gym/staging/data/gcs_cache/mouse_gastrulation/atlas_data.tar.gz`;
the local tar/cache was removed after verification.

Artifacts:

```text
artifacts/scripts/ingest_temporal_mouse_gastrulation_stream_mtx_20260622.py
artifacts/scripts/ingest_temporal_mouse_gastrulation_full_chunks_20260622.py
artifacts/scripts/verify_t13g_mouse_gastrulation_full_20260622.py
artifacts/schema_audit/temporal_t13f_mouse_gastrulation_stream_smoke_20260622.json
artifacts/schema_audit/temporal_t13f_mouse_gastrulation_stream_lamin_20260622.md
artifacts/schema_audit/temporal_t13f_mouse_gastrulation_stream_lamin_20260622.json
artifacts/schema_audit/temporal_t13g_mouse_gastrulation_full_status_20260622.md
artifacts/schema_audit/temporal_t13g_mouse_gastrulation_full_chunks_20260622.json
artifacts/schema_audit/temporal_t13g_mouse_gastrulation_final_verify_20260622.json
```

## Temporal T36 GEO/CNGB spatial development batch — 2026-06-22

T36-Mac (`t_2fa866a6`) performed source/duplicate probes for rows 143–145 and
ingested the bounded row 145 craniosynostosis spatial timecourse. Verified
triplets:

```text
temporal_pretraining/gse303344_craniosynostosis/e14_5_2um   4,671,144 × 19,059   E14.5 Visium, spatial coords complete
temporal_pretraining/gse303344_craniosynostosis/e18_5_2um     532,208 × 19,059   E18.5 Visium, spatial coords complete
temporal_pretraining/gse303344_craniosynostosis/p3_2um        340,320 × 19,059   P3 Visium, spatial coords complete
```

Artifacts:

```text
artifacts/schema_audit/temporal_t36_spatial_source_probe_20260622.md
artifacts/schema_audit/temporal_t36_spatial_source_probe_20260622.json
artifacts/schema_audit/temporal_t36_gse303344_craniosynostosis_20260622.md
artifacts/schema_audit/temporal_t36_gse303344_craniosynostosis_20260622.json
artifacts/scripts/probe_temporal_t36_spatial_sources_20260622.py
artifacts/scripts/ingest_temporal_t36_gse303344_craniosynostosis_20260622.py
```

Current decision:

- Row 143 (`GSE313896`, postnatal vascularization in mouse brain) is
  `source_resolved_not_ingested`: GEO exposes `GSE313896_RAW.tar` at ~2.4 GB with
  46 Visium samples plus per-sample spatial files. Continue with a staged/chunked
  extractor; do not blindly download/convert the full archive while other
  temporal workers are active.
- Row 144 (`CNP0002220`) is `already_represented_by_ZESTA_STDS0000057_5hpf`:
  the CNGB project accession resolves to the ZESTA/STDS0000057 raw project, and
  `temporal_pretraining/zesta_stds0000057/zf5_stereoseq` is already accepted.
  Remaining distinct ZESTA timepoints belong to the ZESTA continuation card, not
  a duplicate raw-project prefix.
- Row 145 (`GSE303344`) is `ingested_verified`: source GEO RAW tar and spatial
  zips were bounded; the three stage triplets above have payloads and obs→X→var
  links verified, with `spatial_coord_x/y` populated for every obs row.

## Temporal T37 human/axolotl/planarian spatial regeneration batch — 2026-06-22

T37-Mac (`t_ca492e7d`) completed source/duplicate probes for rows 146, 148, and
149, then ingested/repaired one bounded ARTISTA/STDS0000056 axolotl telencephalon
Stereo-seq slice as a verified same-prefix triplet:

```text
temporal_pretraining/artista_stds0000056/10DPI_1_stereoseq   9,440 × 27,600   10DPI, spatial coords complete
```

Payload existence plus `obs -> X -> var` links are verified. The source
`10DPI_1.h5ad` cannot be read directly through `anndata.read_h5ad()` because HDF5
metadata under `/uns` is corrupt, but the core X/obs/var/spatial groups were
readable with `h5py`; the canonical triplet was reconstructed from those groups.
The current obs artifact is triplet-valid but lacks `dataset_id`, making it a
future obs-only schema harmonization candidate if temporal datasets require that
column uniformly.

Artifacts:

```text
artifacts/schema_audit/temporal_t37_spatial_regeneration_status_20260622.md
artifacts/schema_audit/temporal_t37_spatial_regeneration_status_20260622.json
artifacts/schema_audit/temporal_t37_artista_triplet_verification_20260622.json
artifacts/scripts/verify_t37_artista_triplet_20260622.py
```

Current decision:

- Row 146 (`GSE326326`) is `source_resolved_large_archive_not_ingested`: GEO
  exposes a byte-range-capable `GSE326326_RAW.tar` of ~2.43 TB containing spatial
  HESTA h5ad/GEF/TIFF payloads. Do not download/extract the full tar locally;
  build a range-aware member lister/extractor and stage selected members first.
- Row 148 (`STDS0000056`) is `partial_ingested_verified`: continue remaining
  ARTISTA sections only in bounded batches, and reuse the h5py reconstruction path
  if `.h5ad` metadata corruption recurs.
- Row 149 (`STAPR`) is `source_page_reachable_not_ingested`: static crawl found
  only the dynamic Downloads tab. Use browser/API inspection to resolve concrete
  processed matrix/spatial metadata URLs before any Lamin write.

## Temporal T38 ZESTA continuation + zebrafish heart probe — 2026-06-22

T38-Mac (`t_4c3abcd9`) completed the bounded ZESTA/STDS0000057 Stereo-seq
timepoint continuation. The prior zf24 transfer blocker was resolved by resuming
explicit ranged downloads, then writing and read-back verifying the same-prefix
triplet. All six ZESTA timepoint prefixes are now verified:

```text
temporal_pretraining/zesta_stds0000057/zf3_stereoseq
temporal_pretraining/zesta_stds0000057/zf5_stereoseq
temporal_pretraining/zesta_stds0000057/zf10_stereoseq
temporal_pretraining/zesta_stds0000057/zf12_stereoseq
temporal_pretraining/zesta_stds0000057/zf18_stereoseq
temporal_pretraining/zesta_stds0000057/zf24_stereoseq
```

STT0000071 regenerating zebrafish heart is source-blocked, not ingested. Metadata is
resolved (`5.69GB`, 569,896 cells/spots, 36 scRNA-seq libraries, 224 Stereo-seq
slices, 8 timepoints; relation `CNP0005245`) and sample search resolves 9 public
zebrafish-heart sample/timepoint records, but those samples are `is_download=false`
with `files=[]`; STSP `sequencing_tableview` / `gene_expression_tableview` probes
return zero rows for the project, relation, and sample ids; and Lamin duplicate
probe terms found no existing STT0000071/CNP0005245/zebrafish-heart artifact keys.
No heart Lamin writes were attempted without a concrete public expression manifest.

Artifacts:

```text
artifacts/schema_audit/temporal_t38_zesta_heart_partial_status_20260622.md
artifacts/schema_audit/temporal_t38_zesta_heart_partial_status_20260622.json
artifacts/schema_audit/temporal_t38_heart_source_probe_20260622.md
artifacts/schema_audit/temporal_t38_heart_source_probe_20260622.json
artifacts/scripts/ingest_t38_zesta_remaining_stomics_20260622.py
artifacts/scripts/verify_t38_zesta_partial_20260622.py
artifacts/scripts/probe_t38_heart_source_20260622.py
```

## Temporal T18 BioStudies/ArrayExpress embryo batch B — 2026-06-22

T18-Mac (`t_a8a338f8`) resolved rows 63, 69, and 70. The bounded rabbit embryo
PGC dataset `E-MTAB-10894` was ingested and verified as
`temporal_pretraining/gxa/E-MTAB-10894_rabbit_embryo_pgc` with shape `381 ×
16,587`; payloads and `obs -> X -> var` links are verified. The matrix was built
from GXA `quantification-raw` MatrixMarket files and filtered to the 381 assay
columns present in the matrix; obs age counts are 4 day: 10, 5 day: 7, 6 day: 85,
7 day: 279.

Artifacts:

```text
artifacts/scripts/probe_t18_biostudies.py
artifacts/scripts/check_t18_duplicates.py
artifacts/scripts/ingest_t18_gxa_rabbit.py
artifacts/scripts/verify_t18_gxa_rabbit.py
artifacts/schema_audit/temporal_t18_biostudies_probe_20260622.json
artifacts/schema_audit/temporal_t18_biostudies_batch_b_status_20260622.md
artifacts/schema_audit/temporal_t18_biostudies_batch_b_status_20260622.json
artifacts/schema_audit/temporal_t18_biostudies_batch_b_verification_20260622.json
```

Current decision: `E-MTAB-8894` human fetal LGE and `E-GEOD-234602` Drosophila
organogenesis are source-resolved through GXA MatrixMarket endpoints but were not
downloaded or ingested. Their experiment-design TSVs are large (30.9 MB and 98.8
MB) and raw/normalised zip HEAD probes timed out after 20 seconds, while
BioStudies exposed only IDF/SDRF or no valid accession API. Continue them only
with a staged/backed GXA MatrixMarket converter; do not ingest SDRF-only metadata
or blindly download generated archives on the Mac mini.

## Temporal T25 OrganoidDB mesoderm/heart/intestine batch — 2026-06-22

T25-Mac (`t_a61d362c`) resolved and ingested all three OrganoidDB rows in the
batch. Verified triplets:

```text
temporal_pretraining/organoid/odd001154_gse194214_paraxial_mesoderm_somitoids        18,716 × 33,694   days 1/2/3/5
temporal_pretraining/organoid/odd001137_gse158999_mouse_gastruloid_cardiogenesis    30,496 × 23,961   days 4/5/6/7
temporal_pretraining/organoid/odd001151_gse148093_mouse_intestinal_organoids         5,212 × 24,870    sample days 0/1/2/3
```

Payload existence and `obs -> X -> var` links were verified for all three. For
`Odd001151 / GSE148093`, ingestion intentionally used only the OrganoidDB-listed
GSM4453981–GSM4454011 samples; later unrelated/additional GEO RAW tar members
were left out of this canonical row.

Artifacts:

```text
artifacts/scripts/ingest_temporal_organoiddb_t25_20260622.py
artifacts/schema_audit/temporal_organoiddb_t25_ingestion_20260622.md
artifacts/schema_audit/temporal_organoiddb_t25_ingestion_20260622.json
```

## Temporal T26 OrganoidDB / Human Organoid Bank batch — 2026-06-22

T26-Mac (`t_0b093cca`) resolved rows 91, 97, and 108. `Odd001111 / GSE130238`
(cerebral cortex organoid months 1/3/6/10) was ingested and verified as
`temporal_pretraining/organoiddb/GSE130238_cortical_organoid_months` with shape
`16,086 × 33,694`; payloads and `obs -> X -> var` links are verified. `Odd001138 /
GSE162547` was source-resolved but not ingested because the GEO RAW archive
contains raw Cell Ranger matrices with `33,939 × 6,794,880` barcode-universe
columns per sample, not filtered cell matrices. The scHOB portal/static pages were
crawled but no concrete `.h5ad`, MatrixMarket, tar/zip, GEO, or ArrayExpress
payload URL was found for a canonical single-cell Human Organoid Bank dataset.

Artifacts:

```text
artifacts/scripts/ingest_temporal_organoiddb_t26_20260622.py
artifacts/schema_audit/temporal_organoiddb_t26_status_20260622.md
artifacts/schema_audit/temporal_organoiddb_t26_status_20260622.json
artifacts/schema_audit/temporal_organoiddb_t26_gse130238_ingest_20260622.json
```

Current decision: do not retry `GSE162547` as a normal 10x filtered matrix. It
needs author-provided filtered barcodes/cell-calling metadata or a reviewed
streaming converter that filters true cells before Lamin registration; blindly
registering millions of raw droplets would corrupt the temporal query surface.
scHOB needs a deeper table/API/detail-page resolver before any ingestion.

## Temporal T21 GEO repair/source batch — 2026-06-22

T21-Mac (`t_c329fc94`) completed bounded source and duplicate probes for rat
kidney postnatal row 71, mouse retina sodium-iodate row 119, and rabbit wound
repopulation row 131. No Lamin writes were performed.

Artifacts:

```text
artifacts/scripts/probe_t21_geo_sources_20260622.py
artifacts/scripts/ingest_temporal_gse328779_rabbit_wound_20260622.py
artifacts/schema_audit/temporal_t21_geo_source_probe_20260622.json
artifacts/schema_audit/temporal_gse328779_rabbit_wound_20260622.json
artifacts/schema_audit/temporal_t21_geo_repair_status_20260622.md
artifacts/schema_audit/temporal_t21_geo_repair_status_20260622.json
```

Current decision:

- Row 71 / rat kidney: do not use `GSE173650` for this catalogue row. Live GEO
  title is “An Atlas of Gene Regulatory Elements in Adult Mouse Cerebrum”, and
  the supplementary archive contains `.snap.gz`/metadata snATAC payloads. The
  plausible rat kidney source remains `PRJNA649702`, but prior and refreshed
  probes found only raw SRA-scale data, not a processed cell×gene matrix.
- Row 119 / `GSE312010`: fresh re-download of `GSE312010_RAW.tar` from GEO had
  the expected 68,761,600 bytes and sha256
  `73dd7d61ebc7a4bd7212c20a0d459398d55a40f604d4bac6d47e08459c259999`, but gzip
  CRC/EOF validation still failed for the day-3 matrix and day-7 features/matrix
  members. Treat this as a source/archive repair blocker; do not ingest partial
  0-day-only data.
- Row 131 / `GSE328779`: source and temporal metadata are good (rabbit cornea,
  transcorneal freeze injury vs contralateral controls at Day 7/Day 28) and exact
  duplicate probes were clean. However, exposed GEO files are
  `raw_feature_bc_matrix.h5`; one-sample smoke produced 6,794,880 barcodes ×
  23,407 features, i.e. raw droplets rather than filtered cells. This needs
  author-provided filtered matrices/barcodes or a reviewed cell-calling/filtering
  workflow before triplet ingestion.

## Temporal T20 GEO developmental mammal/vertebrate batch — 2026-06-22

T20-Mac (`t_7ed6d66c`) completed metadata-only GEO source probes and read-only
Lamin duplicate probes for rows 62, 73, and 78. One bounded dataset was ingested;
the larger two were deferred with explicit staging/conversion requirements.

Artifacts:

```text
artifacts/scripts/probe_temporal_t20_geo_developmental_20260622.py
artifacts/scripts/ingest_temporal_gse280655_cortical_inhibitory_neurons_20260622.py
artifacts/schema_audit/temporal_t20_geo_developmental_source_probe_20260622.json
artifacts/schema_audit/temporal_t20_geo_developmental_source_probe_20260622.md
artifacts/schema_audit/temporal_gse280655_cortical_inhibitory_neurons_20260622.json
artifacts/schema_audit/temporal_t20_geo_developmental_status_20260622.md
artifacts/schema_audit/temporal_t20_geo_developmental_status_20260622.json
```

Current decision:

- Row 73 / `GSE280655` mouse cortical inhibitory neurons was safely ingested from
  the single bounded 88.9 MB GEO RAW 10x archive. Verified prefix:
  `temporal_pretraining/developing_mouse_cortical_inhibitory_neurons/GSE280655_E18_5`
  with `8,015 × 21,909`, payload existence true, and obs→X→var links verified.
  This is a single E18.5 developmental snapshot (`timepoint_unit=embryonic_day`),
  not a multi-timepoint trajectory.
- Row 62 / `GSE334273` sea lamprey sexual differentiation exposes four 10x MTX
  samples inside `GSE334273_RAW.tar` (1.19 GB) plus a 40.7 MB cell metadata file.
  Exact accession duplicate probes were clean, but ingestion should use a
  staged/streamed 10x converter rather than blind local extraction.
- Row 78 / `GSE325829` human embryos after gastrulation exposes a direct processed
  `GSE325829_snRNA_final.h5ad` (11.08 GB) with CS19/CS20/CS23 samples. Exact
  accession duplicate probes were clean, but it needs GCS staging and backed or
  chunked h5ad ingestion; do not full-load locally.

## Temporal T33 axolotl regeneration probe — 2026-06-22

T33-Mac (`t_a13812f2`) completed source/duplicate probes for axolotl regeneration rows 134–136. No bulk downloads and no Lamin writes were performed.

Artifacts:

```text
artifacts/scripts/probe_temporal_t33_axolotl_20260622.py
artifacts/scripts/write_temporal_t33_axolotl_status_20260622.py
artifacts/schema_audit/temporal_t33_axolotl_source_probe_20260622.json
artifacts/schema_audit/temporal_t33_axolotl_status_20260622.md
artifacts/schema_audit/temporal_t33_axolotl_status_20260622.json
```

Current decision:

- Row 134 adult axolotl limb regeneration: resolved primary candidate `GSE165901` (`Fibroblast Dedifferentiation as a Determinant of Successful Regeneration`, PMID 34004152). GEO exposes only `GSE165901_RAW.tar` (1.97 GB); the axolotl-only members are bounded (24 MatrixMarket/feature/barcode files, ~959 MB compressed across 8 samples; stages include LBst50/LBst52/LBst54, BL5dpa, BL11dpa) but need a selective tar extractor and MatrixMarket smoke before Lamin write. `GSE206235` was rejected for this row because its exposed supplement is a 21 MB differential-expression TSV, not a canonical cell×gene matrix.
- Row 135 SciLifeLab timecourse: `rp58y-78935` is a ShinyCell app. The linked GitHub repo `RegenImm-Lab/SciLifeLabServe_limb_data` contains scripts and names original Seurat RDS objects (`20251212.merged_copy.rds`, `20251212.immune_filtered_copy.rds`, edited 20260421 variants), but does not publish those RDS/data files. Needs original app/storage RDS payload resolution before conversion.
- Row 136 `SCP499` Early-Bud Blastema: SCP public site API exposes a bounded manifest (`EB.matrix.txt.gz` 40.4 MB, `EB.idents.txt` 9 KB, `EB.coordinates.txt` 44 KB), but both `download` and `stream` endpoints returned HTTP 401 for tiny metadata and matrix URLs. Needs browser/session-aware SCP export.

## Temporal T13 Zebrahub + mouse gastrulation — 2026-06-22

T13-Mac (`t_152cb737`) completed the Zebrahub stage continuation and performed a
bounded mouse gastrulation HCA archive inspection. Artifacts:

```text
artifacts/schema_audit/temporal_t13_final_status_20260622.md
artifacts/schema_audit/temporal_t13_final_status_20260622.json
artifacts/schema_audit/temporal_t13_zebrahub_all_stages_verification_20260622.json
artifacts/schema_audit/temporal_t13_mouse_gastrulation_archive_inspection_downloaded_20260622.json
artifacts/schema_audit/temporal_t13_mouse_gastrulation_member_schema_probe_20260622.json
```

Current decision:

- Zebrahub is now complete at the stage-triplet level: all 11 stage prefixes
  (`10hpf`, `12hpf`, `14hpf`, `15hpf`, `16hpf`, `19hpf`, `24hpf`, `2dpf`,
  `3dpf`, `5dpf`, `10dpf`) verify with payload existence, required temporal obs
  fields, and same-prefix obs→X→var links. Total verified obs rows: 124,306.
  This run added/verified the seven remaining stages and repaired two issues: a
  partial `3dpf` var/link write after a Lamin dataframe-cache failure, and a
  pre-existing `14hpf` X→`15hpf/var.parquet` link that now points to
  `14hpf/var.parquet`.
- Mouse gastrulation now has a verified streaming-converter smoke, but not full
  ingestion. The HCA/Azul `atlas_data.tar.gz` was downloaded/hash-verified
  (`1,669,736,540` bytes, sha256
  `0b1a69606d451de95f861921d9ec4d5ab1ee6b58ffc500c5ca261db26b42fba8`); T13F
  wrote and verified the first 100 cell columns as
  `temporal_pretraining/mouse_gastrulation_hca/chunk_00000_00099` (`100 × 29,452`,
  344,772 nnz, E6.5). Full ingestion remains a future sequential chunking job over
  all 139,331 cells; do not use `scipy.io.mmread`, full-load conversion, blind
  archive unpacking, or parallel writers for this MTX.

## Temporal T17 BioStudies/ArrayExpress embryo batch — 2026-06-22

T17-Mac (`t_c8478c79`) corrected the earlier BioStudies-only conclusion by probing
Single Cell Expression Atlas processed-export directories for rows 59, 64, and 72.
Artifact:

```text
artifacts/schema_audit/temporal_t17_biostudies_embryo_batch_status_20260622.md
artifacts/schema_audit/temporal_t17_biostudies_embryo_batch_status_20260622.json
```

Current decision:

- Row 64 / `E-MTAB-3929` was ingested and verified as
  `temporal_biostudies/E-MTAB-3929_preimplantation_embryo` (`1,519 × 34,570`),
  with `timepoint` revised to embryonic day labels `3 day`–`7 day` and verified
  obs→X→var links/payloads.
- Row 59 / `E-MTAB-9304` and row 72 / `E-MTAB-8060` now have source-resolved SCEA
  `.project.h5ad`, filtered-count MTX, cell metadata, and condensed SDRF exports.
  They were not ingested in this run because the h5ads are 1.19 GB and 1.96 GB
  respectively; use backed or MatrixMarket-based conversion, or a quiet higher-RAM
  run, rather than full-loading them on the busy 16 GB Mac mini.

T40-Mac row 86 (`Ceratitis capitata` LSFM embryogenesis) now has a metadata-only representation probe:

```text
artifacts/schema_audit/temporal_ceratitis_lsfm_representation_probe_20260622.md
artifacts/schema_audit/temporal_ceratitis_lsfm_representation_probe_20260622.json
```

T31-Mac row 124 (Tian/Kampmann 2019 iPSC/neuron CRISPRi) is `already_present_verified_schema_gap`:

```text
artifacts/schema_audit/temporal_t31_tian_kampmann_status_20260622.md
artifacts/schema_audit/temporal_t31_tian_kampmann_status_20260622.json
```

The broad Zenodo/scPerturb record was resolved to `TianKampmann2019_iPSC.h5ad` and `TianKampmann2019_day7neuron.h5ad`; exact existing triplets `scperturb/tian19_iPSC` and `scperturb/tian19_day7neuron` have verified payloads and obs→X→var links. Do not redownload this bundle unless the existing Lamin payloads are later found corrupt; remaining work is only optional obs-schema harmonization from older `pert_name`/`pert_type`/`pert_time` fields to canonical perturbation fields.

T30-Mac PerturBase reprogramming/endoderm batch (`t_e8313cc5`) is source/duplicate-audited with no Lamin writes:

```text
artifacts/scripts/probe_temporal_t30_perturbase_20260622.py
artifacts/schema_audit/temporal_t30_perturbase_status_20260622.md
artifacts/schema_audit/temporal_t30_perturbase_status_20260622.json
```

Current decision: row 117 (`GSE122662`/Schiebinger-Lander optimal transport) was duplicate-reviewed against existing `SchiebingerLander2019` (`327,494` cells, `27,998` genes, sources `GSE115943`/`GSE106340`, Dox/2i/control perturbations) and was not ingested as a new independent dataset. Row 116 (`GSE107185`) and row 118 (`GSE127202`) were also not ingested: public GEO payloads are accessible, but they do not directly reproduce the PerturBase filtered objects (`extend_61`/`pilot_14` for row 116; 13,165-cell filtered object for row 118). Treat them as converter/source-contract blockers rather than safe missing triplets.


## Temporal T41 CELLxGENE B-candidate high-value continuation — 2026-06-22

T41-Mac (`t_6cc6cd95`) performed a CELLxGENE collection/source probe and bounded ingestion for rows 2, 3, and 8. Artifacts:

```text
artifacts/scripts/probe_temporal_cellxgene_t41_20260622.py
artifacts/scripts/ingest_temporal_cellxgene_t41_20260622.py
artifacts/scripts/repair_temporal_cellxgene_t41_row2_obs_20260622.py
artifacts/schema_audit/temporal_cellxgene_t41_probe_20260622.json
artifacts/schema_audit/temporal_cellxgene_t41_ingestion_20260622.json
artifacts/schema_audit/temporal_cellxgene_t41_ingestion_20260622.md
artifacts/schema_audit/temporal_cellxgene_t41_row2_obs_repair_20260622.json
artifacts/logs/temporal_cellxgene_t41_dryrun_20260622.log
artifacts/logs/temporal_cellxgene_t41_ingest_20260622.log
```

Verified triplets:

- Row 2 / human outflow tract and aortic valve derivatives: `temporal_pretraining/human_outflow_tract_aortic_valve_derivatives/cellxgene_single_nuclei_sn_rna_seq_of_the_human_outflow_tract_and_aortic_valve_tissue_cs16` (`30,125 × 31,008`). Raw temporal labels are `13th week post-fertilization stage`, `Carnegie stage 17`, and `adult stage`; obs→X→var links and all three payloads are verified. A follow-up obs-only revision keeps Carnegie/adult as raw labels and only converts the 13th-week cells to minute-valued `timepoint` (`17,416` non-null rows).
- Row 8 / mouse embryonic timelapse gastrula-to-pup: `temporal_pretraining/mouse_embryonic_timelapse_gastrula_to_pup/cellxgene_major_cell_cluster_testis_and_adrenal` (`3,342 × 45,525`). Raw temporal labels are Theiler stages 18–27, stored with `timepoint_unit_assumption=theiler_stage_order`; obs→X→var links and all three payloads are verified.

Residual decisions: row 3 human fetal gene expression atlas is source/duplicate-probed but not ingested because CELLxGENE exposes ~4.94 GB and ~20.03 GB H5AD assets across ~5.06M reported cells; use a streaming/chunked path, not local full load. Row 8 remaining clusters/full datasets total ~320 GB H5AD across ~34.3M reported cells and should continue with chunked/backed selection after the bounded smoke. Row 2 Visium assets are deferred for the spatial representation contract, not forced into canonical scRNA triplets. Local CELLxGENE cache and Lamin cache payloads were removed after verification.

## Temporal T42 CELLxGENE B-candidate small/partial continuation — 2026-06-22

T42-Mac (`t_51169565`) continued bounded CELLxGENE sub-datasets for rows 5, 10, and 12. Artifacts:

```text
artifacts/scripts/ingest_temporal_cellxgene_t42_continuation_20260622.py
artifacts/scripts/verify_temporal_cellxgene_t42_final_20260622.py
artifacts/schema_audit/temporal_cellxgene_t42_final_status_20260622.json
artifacts/schema_audit/temporal_cellxgene_t42_final_status_20260622.md
artifacts/logs/temporal_cellxgene_t42_final_verify_20260622.log
```

Verified triplets: 9 prefixes / 203,296 obs total, all with payload existence and obs→X→var links verified by final read-back.

- Row 5 / human fetal lung gradients: 7 bounded 10x 5' scRNA subsets beyond the already-ingested PNS subset — fibroblast/smooth muscle (`39,204 × 23,323`), epithelium/no-cilium (`9,096 × 22,385`), cilium (`1,641 × 18,488`), endothelium (`5,467 × 17,605`), B cells (`4,138 × 17,167`), T/NK/ILC (`4,778 × 17,391`), and myeloid (`6,947 × 19,786`).
- Row 10 / fetal bone marrow Down syndrome: `temporal_pretraining/human_fetal_bone_marrow_down_syndrome/cellxgene_fetal_bone_marrow_10x_down_syndrome` (`16,743 × 33,715`).
- Row 12 / mammalian cerebellum: `temporal_pretraining/mammalian_cerebellum_development/cellxgene_snrna_seq_data_for_mouse_cerebella_from_9_12_developmental_stages` (`115,282 × 20,287`).

Residual decisions: this is explicitly small/partial continuation, not full collection completion. Fetal lung Visium assets are deferred for spatial representation, lung Organoid/All-cells exceed the bounded local full-ingest threshold, normal fetal BM CITE-seq/PNS were already ingested, CD34+ fetal BM (606 MB) and all-cells fetal BM (1.75 GB) need chunked/explicit continuation, and human cerebellum (840 MB) needs a chunked/backed path.

## Temporal T29 PerturBase directed differentiation status — 2026-06-22

T29-Mac (`t_22409681`) now has one verified ingestion and two remaining blocked/deferred components. Artifacts:

```text
artifacts/scripts/ingest_temporal_perturbase_t29_gse142078_20260622.py
artifacts/schema_audit/temporal_t29_gse142078_ingestion_20260622.md
artifacts/schema_audit/temporal_t29_gse142078_ingestion_20260622.json
artifacts/schema_audit/temporal_t29_perturbase_directed_differentiation_status_20260622.md
artifacts/schema_audit/temporal_t29_perturbase_directed_differentiation_status_20260622.json
artifacts/scripts/probe_temporal_perturbase_t29_gse216481_20260622.py
artifacts/schema_audit/temporal_t29_gse216481_row113_probe_20260622.md
artifacts/schema_audit/temporal_t29_gse216481_row113_probe_20260622.json
artifacts/schema_audit/temporal_t29_gse216481_filelist_20260622.txt
```

Decision: row 115/GSE142078 is `ingested_verified` at `temporal_pretraining/perturbase/gse142078_luhmes_crispri_day8` (`8,843 × 33,694`, obs→X→var links and payloads OK, 942 controls, 15 perturbation labels including non-targeting). This intentionally keeps the reconstructed guide-joined cells per user decision, while preserving the PerturBase 7,684-cell filtered-count discrepancy in obs `qc_note`; run2/run3 sgRNA/vector pseudo-features are excluded from canonical RNA `X.h5ad` and represented through obs guide fields.

Residual blockers: row 114/GSE156170 is still `blocked_biological_label_qc_ambiguity`. Bounded matrix/pheno inspection found mixed hg19/mm10 archives and pheno dicts that assign many cells to multiple guides; human∩pheno filters do not reproduce PerturBase filtered counts (`neural` 30,065 vs 17,340, `screen` 24,832 vs 24,235, `repool` 34,437 vs 13,424), so a canonical write would risk wrong perturbation labels without an explicit multi-guide/filtered-cell policy. Row 113/GSE216481 has now been staged to `gs://scperturb/pert-gym/staging/data/main/temporal_pretraining/perturbase_t29/GSE216481_RAW.tar` (17,908,162,560 bytes; GCS md5 `F/KU8vFWRdT8CqJSggeYnQ==`, crc32c `4qSU/w==`) and range/filelist-probed without Lamin writes: the RAW tar contains QC-pass RNA components `201218_RNA` (16 dense gene×encoded-cell CSV.gz files plus TFmap; PerturBase filtered `56,857 × 36,844`) and `210322_TFAtlas` (20 dense gene×encoded-cell CSV.gz files plus TFmap; PerturBase filtered `527,594 × 16,873`), plus ATAC and failed/combinatorial components that must not enter canonical RNA `X.h5ad`. Canonical row113 ingestion is blocked on perturbation-label/filter safety, not source size: GEO/PerturBase metadata probed so far exposes encoded `R1/R2/R3/P1` cell coordinates and TFmap barcode/sequence/numeric rows, but no verified barcode/ORF-to-TF-symbol and filtered-cell contract.

Decision: no Lamin ingestion yet. The source is nine Zenodo live 3D+t LSFM image records with ZIP TIFF payloads, AVI previews, and XLSX metadata (~275.6 GB API file sum). Treat this as blocked pending a reviewed image/non-expression contract (`modality=image`, empty `X`, metadata/image URIs in `obs`, optional future `obsm_image_embedding`), not as an expression triplet task.

T35-Mac STOmics spatial-development batch (`t_3404197c`) now has a metadata-only source and duplicate probe:

```text
artifacts/scripts/probe_temporal_t35_stomics_spatial_20260622.py
artifacts/schema_audit/temporal_t35_stomics_spatial_probe_20260622.md
artifacts/schema_audit/temporal_t35_stomics_spatial_probe_20260622.json
```

Decision: no Lamin ingestion yet and no bulk downloads. HESTA (`https://db.cngb.org/stomics/hesta/`) is reachable but the simple HTTP/API probe did not resolve a downloadable manifest or STDS id; it needs a dedicated SPA/source resolver. STDS0000060 (Drosophila embryos/larvae) resolves to five processed Stereo-seq H5ADs, all large (HEAD-confirmed ~1.44, 1.60, 1.78, 5.40, and 7.05 GB), with 0 exact Lamin duplicate hits. MOSTA/STDS0000058 remains a large atlas (187 files, 61 H5ADs, prior/source-confirmed smallest section ~292 MB and all-stage file ~7.44 GB) and needs staged/chunked spatial ingestion preserving coordinates, not blind local sync.

## Temporal T43 Drosophila continuum + human gastrulation — 2026-06-22

T43-Mac (`t_f70e9535`) completed a mixed converter/source-resolution pass. Human gastrulation was small and bounded enough to ingest; Drosophila was source-resolved but intentionally deferred.

Artifacts:

```text
artifacts/scripts/probe_temporal_t43_sources_20260622.py
artifacts/scripts/ingest_temporal_E-MTAB-9388_human_gastrulation_20260622.py
artifacts/scripts/verify_temporal_E-MTAB-9388_human_gastrulation_light_20260622.py
artifacts/schema_audit/temporal_t43_drosophila_human_gastrulation_status_20260622.json
artifacts/schema_audit/temporal_t43_drosophila_human_gastrulation_status_20260622.md
```

Current decision:

- Row 82 `E-MTAB-9388` human gastrulation is `ingested_verified` at prefix
  `temporal_pretraining/human_gastrulation/E-MTAB-9388`. Verified shape is
  `1195 × 25330`, with 6,235,798 nonzeros, same-prefix `obs -> X -> var` links,
  payloads present, `developmental_stage=CS7`, `timepoint=7.0`, and sampling-site
  counts caudal 665 / rostral 340 / yolk sac 190. The source does not expose
  spatial coordinates in the static Atlas files; spatial context is currently
  represented through `sampling_site` and cell-type metadata.
- Row 60 `GSE190149` Drosophila embryogenesis continuum is
  `source_resolved_deferred_large_chunked_converter_needed`. `GSE190149` is a
  SuperSeries: RNA expression is in `GSE190147` with direct MatrixMarket files
  (`23932 × 547805`, 212,703,167 nnz, ~697,773,156-byte gz matrix), and ATAC peak
  matrices are in `GSE190130`. Do not ingest the Drosophila RNA matrix locally
  without GCS staging/chunking; do not force ATAC into canonical RNA `X.h5ad`.

## Temporal T14 SCP small-animal source probe — 2026-06-22

T14-Mac (`t_73201941`) performed a metadata-only source and duplicate probe for
three developmental small-animal Single Cell Portal rows. No SCP bulk downloads
and no Lamin writes were performed.

Artifacts:

```text
artifacts/scripts/probe_temporal_t14_scp_small_animal_20260622.py
artifacts/schema_audit/temporal_t14_scp_small_animal_source_probe_20260622.json
artifacts/schema_audit/temporal_t14_scp_small_animal_source_probe_20260622.md
```

Current decision:

- `SCP667` zebrafish hindbrain: public SCP API exposes 11 files, including six
  matrix/10x-like files, one metadata file, and four cluster files; largest file
  is `gene_sorted-matrix.mtx` (42,287,012 bytes).
- `SCP454` proto-vertebrate/ciona lineages: public SCP API exposes 17 files,
  including eight matrix files and one metadata file; the all-stage expression
  matrix `expression_matrix_10stage.tsv.gz` is 2,096,720,188 bytes, so this needs
  staging/chunking after auth rather than a blind local download.
- `SCP162` zebrafish embryogenesis: public SCP API exposes 7 files, including
  `URD_Dropseq_Expression_Log2TPM.txt.gz` (138,527,834 bytes), metadata, clusters,
  and a 631,079,944-byte Seurat RDS.
- Exact planned prefixes and accession/file-name probes returned no existing
  Lamin artifact hits on `laminlabs/pertdata` branch `jkobject`.
- Tested SCP `download` and `stream` Range probes returned HTTP 401 for all three
  studies. These rows are therefore `blocked_source_auth_needed`: use an
  authenticated/session-aware Single Cell Portal export before conversion or
  Lamin writes.

## Temporal T16 SCP chick/Drosophila/retinal ganglion probe — 2026-06-22

T16-Mac (`t_43b90af5`) performed a metadata-only source and duplicate probe for
SCP1469, SCP1570, and SCP1846. No SCP bulk downloads and no Lamin writes were
performed.

Artifacts:

```text
artifacts/scripts/probe_t16_scp_lamin.py
artifacts/schema_audit/temporal_t16_scp_probe_20260622.json
artifacts/schema_audit/temporal_t16_scp_probe_20260622.md
```

Current decision:

- `SCP1469` Hand-GFP Drosophila embryos: SCP site API exposes 4 files, including
  an expression matrix, gene list, metadata, and cluster file; reported shape
  34,203 cells × 10,686 genes.
- `SCP1570` early chick development: SCP site API exposes 68 files across HH4–HH7
  whole/subset matrix families, genes/barcodes, clusters, and a Seurat RDS;
  reported shape 26,971 cells × 17,557 genes. This needs matrix-family selection
  before any ingestion.
- `SCP1846` retinal ganglion injury/regeneration: SCP site API exposes 8 files,
  including count/data matrices, gene/cell names, cluster, and metadata; reported
  shape 129,441 cells × 23,308 genes.
- All exact planned `temporal_scp/<accession>/obs.parquet|X.h5ad|var.parquet`
  keys had 0 existing hits on `laminlabs/pertdata` branch `jkobject`, and
  accession/title fuzzy key searches returned no duplicate artifact keys.
- All representative SCP `/download?filename=...` endpoints returned HTTP 401
  from headless non-session curl; visiting the public page with a curl cookie jar
  and retrying downloads also returned 401 with empty bodies. These rows are
  therefore `blocked_source_auth_needed` until an authenticated/session-aware SCP
  export is available.

## Temporal T23 OrganoidDB retina/blood-vessel probe — 2026-06-22

T23-Mac (`t_103c7d05`) performed a metadata-only OrganoidDB/GEO source and duplicate probe for three organoid-development rows. No GEO RAW tar downloads and no Lamin writes were performed.

Artifacts:

```text
artifacts/scripts/resolve_t23_organoiddb_sources.py
artifacts/schema_audit/temporal_organoiddb_t23_status_20260622.json
artifacts/schema_audit/temporal_organoiddb_t23_status_20260622.md
```

Current decision:

- `Odd001126 / GSE142526` retina organoid: GEO SOFT resolves 22 samples and parsed day labels including day 45, 78, 105, 122, and 205; exact planned prefix had 0 Lamin duplicate hits.
- `Odd001132 / GSE152212` retina organoid: GEO SOFT resolves 51 samples and parsed day labels including d70, d90/91, d120/122, d150, d190, and d225; exact planned prefix had 0 Lamin duplicate hits.
- `Odd001155 / GSE196799` blood-vessel organoid: GEO SOFT resolves 10 samples and a day-12 label; exact planned prefix had 0 Lamin duplicate hits.
- All three GEO supplementary listings expose only RAW tar/metadata/listing files and no obvious processed cell×gene matrix/barcodes/features/h5ad. These rows are therefore `blocked_processed_matrix_absent_or_raw_only`: find a real processed scRNA matrix outside the exposed GEO supplements or create a raw SRA/alignment converter card before any triplet ingestion.

## Temporal T24 OrganoidDB cortical/kidney/retina batch — 2026-06-22

T24-Mac (`t_9217af79`) resolved three OrganoidDB/GEO rows. It ingested the one bounded processed matrix and recorded concrete blockers for the other two rows.

Verified triplet:

```text
temporal_pretraining/organoid/odd001100_gse118184_kidney_organoid_takasato_ips_timecourse/obs.parquet
temporal_pretraining/organoid/odd001100_gse118184_kidney_organoid_takasato_ips_timecourse/X.h5ad
temporal_pretraining/organoid/odd001100_gse118184_kidney_organoid_takasato_ips_timecourse/var.parquet
```

Verification summary:

- `Odd001100 / GSE118184` human PSC-derived kidney organoid Takasato iPS timecourse: shape `9,190 × 14,821`; `obs -> X -> var` links resolve; payloads exist; all rows have minute-valued `timepoint`; raw labels preserved for `Day0`, `Day7`, `Day12`, `Day19`, and `Day26`; baseline rows are `Day0` (`218` cells). Local GEO gzip and Lamin cache payloads were removed after verification.
- `Odd001097 / GSE106245` human cortical organoid: metadata confirms week 0/1/2/5 scRNA timing, but exposed GEO supplements are bulk-style RSEM/baseMean/annotation files and no processed scRNA matrix/barcodes/features/h5ad is available. Status: `blocked_processed_scrna_matrix_absent`; use an external processed matrix or create a raw-SRA converter/alignment task.
- `Odd001099 / GSE138002` retina organoid/development: processed MatrixMarket/barcode/gene payloads exist, with day 24/30/42/59 organoid labels, but the final matrix is `565 MB` gzip, `33,694 × 118,555` with `178,087,120` nonzeros. Status: `deferred_chunked_converter_needed`; build a streaming/chunked MatrixMarket converter rather than full-loading in a shared Mac run.

Artifacts:

```text
artifacts/scripts/ingest_temporal_t24_organoiddb_20260622.py
artifacts/schema_audit/temporal_t24_organoiddb_20260622.json
artifacts/schema_audit/temporal_t24_organoiddb_20260622.md
artifacts/logs/temporal_t24_organoiddb_dry_run_20260622.log
artifacts/logs/temporal_t24_organoiddb_ingest_20260622.log
```

## Temporal T27 organoid public-download SCP/GEO probe — 2026-06-22

T27-Mac (`t_e004636c`) performed a metadata-only source and duplicate probe for
SCP3697, SCP211, and GSE293573. No SCP bulk downloads and no Lamin writes were
performed.

Artifacts:

```text
artifacts/scripts/probe_temporal_t27_organoid_public_batch_20260622.py
artifacts/schema_audit/temporal_t27_organoid_public_batch_20260622.json
artifacts/schema_audit/temporal_t27_organoid_public_batch_20260622.md
```

Current decision:

- `SCP3697` human brain organoids over multiple years: public SCP record exists
  but exposes `study_file_count=0`, `cell_count=0`, and `gene_count=0`; no public
  expression or metadata payload is available from the record. Status:
  `blocked_source_files_absent`.
- `SCP211` Human Kidney Organoids Atlas: SCP API exposes 31 files, including 1
  expression matrix, 7 MM coordinate matrices with genes/barcodes, 8 cluster
  files, and 1 metadata file. Day labels visible in filenames/descriptions
  include D0/D7/D15/D29/D32/D51, but every probed `download_url` returned HTTP
  401 with no signed redirect. Status: `blocked_source_auth_needed`; use
  authenticated/session-aware SCP export before conversion.
- `GSE293573` micropatterned forebrain organoid platform: GEO SOFT metadata has 2
  scRNA samples (WT and DYRK1A) and SRA relations (`SRX28221047`, `SRX28221048`),
  but no supplementary files or processed matrix payload. Status:
  `blocked_source_files_absent`; resolve an alternate processed repository or
  create a raw-SRA converter card.
- Exact planned Lamin prefixes had 0 existing obs/X/var hits on
  `laminlabs/pertdata` branch `jkobject`, and accession/term duplicate searches
  returned no artifact keys.

## Temporal T28 brain/cardioid/kidney organoid follow-up probe — 2026-06-22

T28-Mac (`t_40508832`) performed a metadata-only source and duplicate probe for
SCP282, GSE269572, and GSE329346. No SCP/GEO bulk downloads and no Lamin writes
were performed.

Artifacts:

```text
artifacts/scripts/probe_temporal_t28_organoid_followup_20260622.py
artifacts/schema_audit/temporal_t28_organoid_followup_status_20260622.json
artifacts/schema_audit/temporal_t28_organoid_followup_status_20260622.md
```

Current decision:

- `SCP282` Reproducible Brain Organoids: prior SCP API probe exposes seven
  expression matrices plus metadata with 3-month and 6-month labels, but every
  unauthenticated `download_url` Range probe returned HTTP 401 with no signed
  redirect. Status: `blocked_source_auth_needed`; use authenticated/session-aware
  SCP export before conversion.
- `GSE269572` stable chambered cardioids from human pluripotent stem cells: GEO
  page/SOFT metadata probe exposed no supplementary processed cell×gene matrix.
  Status: `blocked_no_geo_supplementary_files`; resolve source paper/SRA or an
  alternate processed repository before ingestion.
- `GSE329346` trajectory-resolved human kidney organoid developmental toxicity:
  GEO exposes only `GSE329346_bulk_RNA_seq_geneexpression.txt.gz`, not an scRNA
  matrix/barcodes/features/h5ad payload. Status: `blocked_processed_matrix_absent`;
  find a real processed scRNA matrix/source repository before ingestion.
- Exact planned Lamin prefixes had 0 existing obs/X/var hits on
  `laminlabs/pertdata` branch `jkobject`, and accession/term searches returned no
  duplicate artifact keys.

T39-Mac (`t_16e0d7b6`) now has a metadata-only plant spatial/development source and duplicate probe:

```text
artifacts/schema_audit/temporal_t39_plant_spatial_probe_20260622.md
artifacts/schema_audit/temporal_t39_plant_spatial_probe_20260622.json
artifacts/scripts/probe_t39_plant_spatial_20260622.py
```

Decision: no safe/bounded plant ingestion yet. Tomato callus regeneration
GSE293948 is real and public, but GEO exposes a single ~12 GB processed RDS
(`GSE293948_object_layer_celltype.rds`), so it needs staged/high-memory
Seurat/RDS inspection before any triplet conversion. The Arabidopsis life-cycle
row needs exact repository/accessions beyond the Travis Lee homepage. The
Arabidopsis STOmics row needs an exact STDS/project accession rather than the
generic CNGB datasets page. Duplicate probe on `laminlabs/pertdata` branch
`jkobject` found no `GSE293948`, tomato/Solanum, Arabidopsis, or plant keys;
`STDS` hits were only the existing ZESTA triplet. Future plant work should keep
plant binomial `organism`, preserve plant locus/gene IDs as `feature_id` without
forcing human/mouse gene conventions, and keep spatial coordinates as
metadata/auxiliary fields while canonical `X.h5ad` remains expression.

## Temporal T32 zebrafish regeneration SCP probe — 2026-06-22

T32-Mac (`t_d2e1109a`) performed a metadata-only source and duplicate probe for
the three zebrafish regeneration SCP rows. No SCP bulk downloads and no Lamin
writes were performed.

Artifacts:

```text
artifacts/scripts/probe_temporal_t32_zebrafish_regen_scp_20260622.py
artifacts/schema_audit/temporal_t32_zebrafish_regen_scp_20260622.json
artifacts/schema_audit/temporal_t32_zebrafish_regen_scp_20260622.md
```

Current decision:

- `SCP1549` zebrafish beta-cell regeneration: SCP API exposes 13 files,
  including 2 expression matrices and 1 metadata-like file. Parsed timepoint
  candidates include 0, 2, 7, and 14 dpa; `Control` appears in exposed labels.
- `SCP1674` regenerating fin osteoblasts: SCP API exposes 4 files, including 2
  expression matrices and 1 metadata-like file. `control` appears in exposed
  labels; exact timepoints must be parsed from the metadata after export.
- `SCP1973` zebrafish retina regeneration: SCP API exposes 4 files, including 2
  expression matrices and 1 metadata-like file. `uninjured` appears in exposed
  labels; exact timepoints must be parsed from the metadata after export.
- All exact planned Lamin prefixes had 0 existing obs/X/var hits on
  `laminlabs/pertdata` branch `jkobject`, and accession/term searches returned no
  duplicate artifact keys.
- All probed SCP `download_url` endpoints returned HTTP 401 with no signed
  redirect/access. These rows are therefore `blocked_source_auth_needed`: use an
  authenticated/session-aware Single Cell Portal export before conversion, then
  write same-prefix triplets only after inspecting matrix/metadata format and
  explicit timepoint/control labels.

## Temporal T34 axolotl SCP blastema ingestion — 2026-06-22

T34-Mac (`t_e8fd9411`) ingested and verified the three T34 axolotl blastema
Single Cell Portal rows by using public SCP `Cluster/cell_values` annotations for
exact cell membership and GEO `GSE121737` processed `repGene` matrices for
expression values. The previous T33 concurrency gate is obsolete: T33 did not
write SCP499, and this T34 run wrote only rows 137–139.

Artifacts:

```text
artifacts/scripts/ingest_t34_gse121737_axolotl_blastema_20260622.py
artifacts/schema_audit/temporal_t34_gse121737_axolotl_blastema_ingestion_20260622.json
artifacts/schema_audit/temporal_t34_gse121737_axolotl_blastema_ingestion_20260622.log
artifacts/schema_audit/temporal_t34_gse121737_axolotl_blastema_ingestion_20260622.md
artifacts/schema_audit/temporal_t34_axolotl_blastema_source_probe_20260622.json
artifacts/schema_audit/temporal_t34_axolotl_blastema_source_probe_20260622.md
```

Written triplets on `laminlabs/pertdata`, branch `jkobject`:

- SCP422 intact limb: `temporal_pretraining/gse121737_axolotl_blastema/intact_limb`
  (`3421 × 387212`, 7,119,458 nonzeros).
- SCP500 Medium-Bud Blastema:
  `temporal_pretraining/gse121737_axolotl_blastema/medium_bud_blastema`
  (`12531 × 516932`, 26,470,683 nonzeros), merged from 6,727 cells in
  `GSE121737_early_and_medium_bud.repGene.txt.gz` plus 5,804 cells in
  `GSE121737_wound_healing_and_medium_bud.repGene.txt.gz`.
- SCP489 wound-healing stage:
  `temporal_pretraining/gse121737_axolotl_blastema/wound_healing_stage`
  (`7270 × 410988`, 13,429,276 nonzeros).

Verification:

- SCP cell-value rows matched expected cell counts exactly: 3,421 / 12,531 /
  7,270.
- Every selected SCP cell was present in the GEO matrix headers after normalizing
  SCP names like `i3_intact_S1_bcAAJM` to matrix column `S1_bcAAJM`.
- Duplicate probe before write found 0 exact planned key hits and 0 accession/term
  hits for `GSE121737`, `SCP422`, `SCP500`, `SCP489`, `axolotl`, and `blastema`.
- Post-write read-back verified same-prefix `obs -> X -> var` links, payload
  existence, row/feature counts, and non-null cluster annotations.

Important caveat: the GEO `repGene` matrices are de-novo transcript/contig-level
features (`c..._g..._i...`, sometimes with appended UniProt/domain annotations),
not clean canonical gene-symbol matrices. Keep these triplets as source-faithful
axolotl expression data, but do not treat `var.gene_symbol` as orthologized genes
without a later feature-harmonization pass. T33 row 136 / SCP499 early-bud remains
not ingested; it should use the same source-family pattern if added later.

## Temporal pretraining smoke ingest — 2026-06-21

A bounded v0 smoke ingestion was completed for the first primary T0 candidate, the CELLxGENE human embryonic limb atlas. To avoid bulk ingestion, only the smallest scRNA dataset in the collection was used: `skm_scRNAseq` (`49dbdcb6-f1d5-4423-9999-1e4a6b67d10b`, source shape `12376 × 25769`, H5AD asset 157 MB). The written Lamin triplet is an explicit smoke subset, not a full collection ingestion:

```text
temporal_pretraining/human_embryonic_limb/cellxgene_skm_scrnaseq_smoke_2000/obs.parquet
temporal_pretraining/human_embryonic_limb/cellxgene_skm_scrnaseq_smoke_2000/X.h5ad
temporal_pretraining/human_embryonic_limb/cellxgene_skm_scrnaseq_smoke_2000/var.parquet
```

Verification passed on branch `jkobject`: shape `2000 × 25769`; `obs -> X -> var` links resolve; required temporal obs fields (`timepoint`, `trajectory_id`, `pseudotime`, `is_baseline`) are present; `timepoint` was inferred in minutes from `adj_stage` PCW labels; loader reconstruction worked without a full large load. The temporary 157 MB CELLxGENE H5AD cache was removed after verification; durable data is in Lamin.

Artifacts:

```text
artifacts/scripts/ingest_temporal_cellxgene_limb_smoke_20260621.py
artifacts/schema_audit/temporal_cellxgene_limb_smoke_20260621.json
artifacts/schema_audit/temporal_cellxgene_limb_smoke_20260621.md
```

## Validation artifacts and commands

Query helper implementation and tests:

```text
tools/query_unified_collection.py
tests/test_query_unified_collection.py
artifacts/schema_audit/validate_unified_collection_queries_20260621.py
artifacts/schema_audit/unified_collection_query_validation_20260621.md
```

Notebook/smoke deliverables:

```text
notebooks/explore_unified_pertdata_collection.ipynb
artifacts/scripts/smoke_explore_unified_pertdata_collection.py
artifacts/schema_audit/explore_unified_collection_smoke_20260621.json
artifacts/schema_audit/explore_unified_collection_notebook_20260621.md
```

Final review reran these checks on 2026-06-21:

```bash
uv run --extra dev python -m pytest tests/test_query_unified_collection.py
uv run python artifacts/schema_audit/validate_unified_collection_queries_20260621.py
uv run python artifacts/scripts/smoke_explore_unified_pertdata_collection.py
```

Results:

- tests: `3 passed`
- validation script: exit 0; collection members 1056; manifest rows 1056; public DRUG-seq, PRISM GSE225775, VIPerturbSeq binA, and DepMap/CCLE examples query cleanly
- notebook smoke: exit 0; collection members 1056; category counts 60 base-public / 996 additions; all 1056 rows `triplet-integrity-ok`; public DRUG-seq resolves `obs -> X -> var` and safely loads tiny X; PRISM resolves `obs -> X -> var` metadata-only by design

Warnings observed during validation were non-blocking Lamin/AWS/provenance warnings: missing optional `botocore[crt]` for default AWS credential provider fallback and `ln.track()` warnings. The checks still connected to `laminlabs/pertdata` branch `jkobject` and completed successfully.

## Temporal pretraining full ingest — 2026-06-22

The reviewed smoke prefix above was upgraded to the full smallest scRNA dataset from the same CELLxGENE human embryonic limb collection. The full triplet is deliberately separate from the smoke subset:

```text
temporal_pretraining/human_embryonic_limb/cellxgene_skm_scrnaseq_full/obs.parquet
temporal_pretraining/human_embryonic_limb/cellxgene_skm_scrnaseq_full/X.h5ad
temporal_pretraining/human_embryonic_limb/cellxgene_skm_scrnaseq_full/var.parquet
```

Verification passed on branch `jkobject`: source/full shape `12376 × 25769`; `obs -> X -> var` links resolve to the full-prefix artifacts; payload existence is true for obs, X, and var; required temporal obs fields (`timepoint`, `trajectory_id`, `pseudotime`, `is_baseline`, `raw_time_label`) are present; all 12,376 rows have PCW-derived `timepoint`; raw labels remain preserved in `raw_time_label`; loader reconstruction worked. This prefix is now the first model-ready candidate for temporal pretraining evaluation, while `*_smoke_2000` remains only a loader/write smoke artifact.

Artifacts:

```text
artifacts/scripts/ingest_temporal_cellxgene_limb_full_20260622.py
artifacts/scripts/repair_verify_temporal_cellxgene_limb_full_20260622.py
artifacts/schema_audit/temporal_cellxgene_limb_full_20260622.json
artifacts/schema_audit/temporal_cellxgene_limb_full_20260622.md
```

Implementation note: the initial full write created full obs/X, then Lamin hash-deduplicated the identical var table against the smoke var artifact. The repair script created the same-prefix full `var.parquet` with `skip_hash_lookup=True` and re-linked X -> var. Local CELLxGENE and Lamin caches were removed after verification.

## Temporal CELLxGENE development batch 1 — 2026-06-22

T7-Mac ingested one conservative sub-500MB CELLxGENE scRNA/CITE-seq expression dataset from each requested developmental collection. All three were resolved through the CELLxGENE curation API, duplicate-probed against `laminlabs/pertdata` branch `jkobject`, inspected in backed mode, written as same-prefix obs/X/var triplets, and verified for payload existence plus `obs -> X -> var` links. Larger H5ADs and Visium assets remain explicit collection-level blockers/deferred assets in the JSON/Markdown artifact rather than being downloaded blindly.

Verified triplets:

```text
temporal_pretraining/human_thymic_development/cellxgene_human_thymic_epithelial_cells_a_cell_atlas_of_human_thymic_development_defines_t/obs.parquet
temporal_pretraining/human_thymic_development/cellxgene_human_thymic_epithelial_cells_a_cell_atlas_of_human_thymic_development_defines_t/X.h5ad
temporal_pretraining/human_thymic_development/cellxgene_human_thymic_epithelial_cells_a_cell_atlas_of_human_thymic_development_defines_t/var.parquet
temporal_pretraining/human_fetal_lung_gradients/cellxgene_pns/obs.parquet
temporal_pretraining/human_fetal_lung_gradients/cellxgene_pns/X.h5ad
temporal_pretraining/human_fetal_lung_gradients/cellxgene_pns/var.parquet
temporal_pretraining/human_fetal_bone_marrow_down_syndrome/cellxgene_human_fetal_bone_marrow_cite_seq/obs.parquet
temporal_pretraining/human_fetal_bone_marrow_down_syndrome/cellxgene_human_fetal_bone_marrow_cite_seq/X.h5ad
temporal_pretraining/human_fetal_bone_marrow_down_syndrome/cellxgene_human_fetal_bone_marrow_cite_seq/var.parquet
```

Verification summary:

- `human_thymic_development` / `Human Thymic Epithelial Cells - A cell atlas of human thymic development defines T cell repertoire formation`: shape `18524 × 32088`; temporal source `development_stage`; numeric timepoint non-null/min/max `4460` / `90720.0` / `7889400.0`; raw labels preserved in `raw_time_label`; payloads `{'X': True, 'obs': True, 'var': True}`.
- `human_fetal_lung_gradients` / `PNS`: shape `649 × 16035`; temporal source `development_stage`; numeric timepoint non-null/min/max `187` / `90720.0` / `221760.0`; raw labels preserved in `raw_time_label`; payloads `{'X': True, 'obs': True, 'var': True}`.
- `human_fetal_bone_marrow_down_syndrome` / `Human Fetal Bone Marrow (CITE-seq)`: shape `8978 × 35469`; temporal source `development_stage`; numeric timepoint non-null/min/max `8978` / `219150.0` / `219150.0`; raw labels preserved in `raw_time_label`; payloads `{'X': True, 'obs': True, 'var': True}`.

Artifacts:

```text
artifacts/scripts/probe_temporal_cellxgene_development_batch1_20260622.py
artifacts/scripts/ingest_temporal_cellxgene_development_batch1_20260622.py
artifacts/schema_audit/temporal_cellxgene_development_batch1_probe_20260622.json
artifacts/schema_audit/temporal_cellxgene_development_batch1_20260622.json
artifacts/schema_audit/temporal_cellxgene_development_batch1_20260622.md
artifacts/logs/temporal_cellxgene_development_batch1_dry_run_20260622.log
artifacts/logs/temporal_cellxgene_development_batch1_dry_run2_20260622.log
artifacts/logs/temporal_cellxgene_development_batch1_ingest_20260622.log
```

Implementation note: Carnegie stage labels are preserved but not converted into minute-valued `timepoint`; gestational week/LMP-month/postnatal age labels are converted where explicit. Local selected H5AD downloads and Lamin cache payloads were removed after verification.

## Model-ready v0 subset — 2026-06-21

The first `pert-gym/model-ready/20260621` collection is intentionally tiny and separate from the 1056-member canonical query surface.

Artifacts:

```text
artifacts/schema_audit/define_model_ready_subset_20260621.py
artifacts/schema_audit/model_ready_subset_20260621.json
artifacts/schema_audit/model_ready_subset_20260621.md
artifacts/schema_audit/model_ready_subset_manifest_20260621.tsv
```

v0 criteria:

- required obs fields: `perturbation`, `perturbation_type`, `organism`, `modality`, `assay`, `is_control`;
- at least one context field from `cell_line`, `cell_type`, `tissue`, `disease`;
- `is_control` must include both control and treated/non-control rows in the promoted member;
- promoted members must be small (`n_obs <= 5000`), and X is smoke-loaded only for tiny examples (`n_obs <= 1000`, `n_vars <= 75000`);
- no full PRISM, T-cell GWPS, or other huge matrices are loaded.

Promotion result:

- created/read-back OK: `pert-gym/model-ready/20260621`, uid `Wh15NEPlEfF86XaO0000`, 1 member;
- promoted member: `viperturb/vimentin_screen_chunk_smoke/chunk_0000/obs.parquet`;
- smoke evidence: obs `(1000, 20)`, var `(17362, 8)`, X `(1000, 17362)`, 28 controls, 972 treated/non-control rows, required obs fields present.

## MB1-Mac classical model benchmark — 2026-06-22

The canonical model-ready-v0 loader contract has now been benchmarked with the
trivial/classical baseline set in the sklearn-isolated `classical` extra. This is
a loader-contract benchmark, not a claim of biological performance: the current
model-ready-v0 command reads manifest metadata only and uses the deterministic
12-row synthetic fallback until a bounded materialized matrix adapter exists.

Artifacts:

```text
tools/run_classical_benchmark.py
artifacts/model_benchmarks/classical_20260622.json
artifacts/model_benchmarks/classical_20260622.md
```

Command:

```bash
uv run --extra classical --extra dev python tools/run_classical_benchmark.py \
  --manifest artifacts/schema_audit/model_ready_subset_20260621.json \
  --artifact-dir artifacts/model_benchmarks \
  --date 20260622
```

Result summary: MeanControl, MeanPerturbation, BinarySplit,
Linear/Ridge/ElasticNet, RandomForest, and GradientBoosting all ran on the test
split with R2/Pearson/MSE recorded. Non-control perturbation identities remain
disjoint across train/val/test (`pert_a,pert_b` / `pert_c` / `pert_d,pert_e`),
controls are copied into every split, and the safety check reports 48 split-matrix
cells with metadata-only synthetic fallback — no Lamin writes and no huge matrix
load. On this tiny synthetic split, LinearPerturbationRegressor had the best R2
(`0.052602`; Pearson `0.564868`; MSE `0.228662`).

Reviewed-but-blocked examples remain explicit in the manifest: public/base examples need alias projection/revision before model-ready (`pert_name`/`pert_type` → `perturbation`/`perturbation_type`, explicit `modality`, `is_control`); one public Cellarity metadata payload is missing remotely; PRISM tiny chunks are metadata-clean but individual chunks often contain only controls or only treated rows; large Sanger/GDSC screens still miss `modality`/`assay` and exceed the v0 small-payload rule.

## MB4F-Mac chemCPA real molecular tiny benchmark — 2026-06-22

The earlier MB4 gate correctly rejected the generic model-ready-v0 VIPerturb/synthetic loader as a chemCPA input. MB4F adds the missing nuance: pert-gym does contain one small expression-response molecular dataset that can support an honest tiny chemCPA-ready smoke.

Artifacts:

```text
tools/build_chemcpa_drugseq_tiny.py
tools/smoke_chemcpa_drugseq_tiny.py
artifacts/model_benchmarks/chemcpa_drugseq_tiny_20260622.json
artifacts/model_benchmarks/chemcpa_drugseq_tiny_20260622.md
```

Result: `DRUG-seq/GSE120222` is exported as a 72-row real molecular expression benchmark with 18 DMSO controls, 6 treated compounds, 128 top-variable expression features, PubChem SMILES/InChI/InChIKey/CID metadata, and 256-bit RDKit Morgan fingerprints. The isolated chemCPA env smoke passed with real loader data: train/val/test rows `45/36/27`, expression features `128`, fingerprint bits `256`. The reported mean-control MAE/RMSE are smoke metrics only, not a trained chemCPA biology claim.

Inventory distinction: `broad_prism_repurposing` and `sanger_gdsc/gdsc1|gdsc2` are drug response/sensitivity screens (`lfc`, `ic50`, `auc`) and remain response-model candidates, not expression-conditioned chemCPA data until baseline expression joins and reviewed structure joins exist. LINCS/Tahoe chemical signatures may be future candidates but were not promoted in this tiny smoke.

## Historical metadata-only audit

The 2026-06-18 metadata-only schema audit remains useful as historical repair context, not as the real P3 completion criterion.

Latest metadata-only status before P3R replacement:

- artifacts: 2429
- triplet prefixes: 720
- complete triplets: 711
- incomplete triplets / partial prefixes: 9
- training candidate prefixes: 711
- logical datasets: 110
- true urgent triplet repairs with no linked var: 0
- same-prefix var missing but `X.features["var"]` already links to shared var: 0
- auxiliary standardization items: 3
- sidecar/reference classification items: 3
- orphan review items: 3
- duplicate candidates: 33
- sampled payload prefixes in the metadata-only run: 0
- metadata-only obs coverage prefixes: 711
- metadata-only var coverage prefixes: 720
- metadata-only X semantics prefixes: 720
- obs prefixes with derivable canonical aliases: 60

Status artifact:

```text
artifacts/schema_audit/schema_harmonization_status_20260618.md
```

## Urgent true triplet repairs

Resolved as of 2026-06-18. The metadata-only audit reports:

- urgent triplet repairs with no linked var: 0
- same-prefix var missing but linked to shared var: 0

The repaired prefixes were:

1. `cellarity/GSE305979/GSE305979_day1-7_raw_counts`
2. `scperturb/replogle22_RPE1`
3. `lincs/phase1/Level2_GEX_delta_n49216x978`
4. Tahoe plates 1-14 under `tahoe100m/2025-02-25/`

Identity evidence lives in the per-repair artifacts and logs under `artifacts/schema_audit/` and `artifacts/logs/`.

## Remaining post-P3 work

These are not blockers for real P3 completion, but they matter before claiming model-ready biological training coverage:

1. Expand `pert-gym/model-ready/<date>` beyond the initial 1-member v0 smoke subset after public/base alias projection, PRISM paired-loader contracts, and additional small reviewed examples are ready.
2. Convert or project the 60 registered derivable obs aliases where needed (`pert_name` -> `perturbation`, `pert_type` -> `perturbation_type`, `pert_dose` -> `dose`, `pert_time`/`time` -> `timepoint`, `self_reported_ethnicity` -> `ethnicity`).
3. Run bounded inspections for the remaining auxiliary/orphan items and convert only confirmed joins to `X_<name>/var_<name>` or `obsm_<name>` artifacts.
4. Run targeted var payload/backed metadata checks for representative families to classify Ensembl/symbol IDs where registry metadata is insufficient.
5. Expand duplicate/subduplicate detection beyond the completed PRISM residual same-accession/GCS gate, especially for model-ready biological coverage decisions.
6. Continue the PRISM P5F residual queue from the current artifacts: 17 accessible smoke-first candidates remain after `GSE247599`; `GSE274751` stays `staged_h5ad_truncated` until re-staged; `GSE247598`, `GSE261157`, `GSE272093`, `GSE272457`, and `GSE282731` remain missing/source-blocked; `GSE90063_human-004` is excluded by user decision due duplicate/subset ambiguity.
7. Continue T-cell GWPS and other huge missing datasets with streaming/GCS/high-memory workers; never sync 100GB+ h5ads blindly.

## P5F-cont9-Mac PRISM residual continuation — 2026-06-23

Status artifacts:

```text
artifacts/schema_audit/prism_GSE247599_chunked_verification.json
artifacts/schema_audit/prism_p5f_cont9_inventory_20260623.json
artifacts/schema_audit/prism_p5f_status_20260622.json
artifacts/phase3_ingestion_progress.json
artifacts/scripts/verify_prism_p5f_dataset_20260623.py
artifacts/scripts/update_prism_p5f_status_after_gse247599_20260623.py
artifacts/logs/prism_GSE247599_full_20260623.log
```

Current decision:

- `GSE247599` was selected by recomputing the current P5F inventory and GCS object sizes, not by trusting the prior handoff. Source: `gs://scperturb/pert-gym/staging/data/main/prism_google_drive_datasets_20260622/GSE247599.h5ad`, 1,433,706,825 bytes.
- Backed inspection showed CSRDataset int64, 24,435 obs x 36,602 vars, duplicate obs names, unique vars, and no obsm keys. The mounted GCS path was too slow for backed open, so the verified repo-local GCS cache helper was used for this 1.4 GB object.
- Chunking used `chunk_size=1000` because the matrix is wide (36k vars) and int64; smoke chunk peak RSS was ~614 MB and full ingestion peak RSS was ~841 MB with no swaps. No full X matrix was loaded.
- Verification passed for 25/25 chunks: 24,435 rows, 36,602 vars, 1,876 controls, all obs->X and X->var links correct, X payloads exist, row/var counts match, and required obs fields are present.
- Live P5F counts after this run: 13 completed, 17 accessible smoke-first candidates, 1 staged payload issue (`GSE274751` truncated), 5 missing/source-blocked rows (`GSE247598`, `GSE261157`, `GSE272093`, `GSE272457`, `GSE282731`), and 1 user-excluded row (`GSE90063_human-004`). Next candidate by size: `GSE283614`.

## P5D-Mac PRISM residual cleanup — 2026-06-22

Status artifacts:

```text
artifacts/schema_audit/prism_residual_cleanup_20260622.md
artifacts/schema_audit/prism_residual_cleanup_20260622.json
artifacts/schema_audit/prism_residual_cleanup_20260622.tsv
artifacts/schema_audit/prism_residual_main_visibility_20260622.json
artifacts/schema_audit/prism_residual_already_present_verification_20260622.json
```

Current decision:

- The stale PRISM residual queue was reclassified after P5B using `phase3_ingestion_progress.json`, current `jkobject` Lamin artifact keys, explicit public/main visibility checks, and GCS object existence. No full X matrices were loaded.
- Public/main explicit check found no exact or same-accession matches for the 40 residual rows. `jkobject` already had complete exact chunked triplets for `GSE208240` and `GSE220974`.
- `GSE208240` and `GSE220974` were verified as already-present PRISM triplets and moved from `downloaded_not_ingested` to `ingested`: `GSE208240` has 74 chunks / 73,989 rows / 33,549 vars / 14,315 controls; `GSE220974` has 5 chunks / 24,661 rows / 23,760 vars / 4,804 controls. Verification checked `obs -> X -> var`, X payload existence, row/var counts, and canonical obs fields without loading X.
- `D4_Rest.assigned_guide` was removed from the PRISM residual queue because it belongs to the T-cell GWPS pipeline, not PRISM; its dedicated T-cell status remains below.
- Remaining PRISM residual queue is 37 rows: 36 are `blocked_source_or_metadata` because no staged GCS h5ad exists and prior Drive/gdown access failed; `GSE90063_human-004` is `needs_human_decision` because same GEO accession overlap was previously observed outside this exact PRISM subset and the source is still unstaged.
- No new PRISM ingestion was launched because the cleanup found no true unique staged PRISM residual that was not already present.

## P5E-Mac PRISM residual Drive/source resolution — 2026-06-22

Status artifacts:

```text
artifacts/schema_audit/prism_drive_residual_resolution_20260622.md
artifacts/schema_audit/prism_drive_residual_resolution_20260622.json
artifacts/schema_audit/prism_drive_residual_resolution_20260622.tsv
artifacts/schema_audit/prism_drive_residual_actual_download_probe_20260622.py
artifacts/schema_audit/prism_drive_residual_access_probe_20260622.json
```

Current decision:

- Mac mini re-tested all 36 P5D `blocked_source_or_metadata` rows with local gdown/cookies and current Google auth; this does not rely on old VPS failures.
- `GSE90063_human-004` is explicitly excluded/skipped by user decision because of duplicate/subset ambiguity.
- No complete residual h5ad was recovered, no residual h5ad was staged to `gs://scperturb/pert-gym/staging/data/main/prism_collection/`, and no new PRISM ingestion was run.
- Final blockers: 36 rows remain `blocked_source_or_metadata` — 31 Google Drive quota/rate-limit blockers and 5 permission/private-link blockers. Exact attempted URLs and error classes are in the TSV/JSON report.
- `GSE236057` and `GSE250558` reached 1 MiB during a bounded body probe but failed full stage-only gdown retries with Drive quota; verification showed no local h5ad and no GCS object for either, so they are not considered recovered.

Next safe action: user/manual authenticated Drive download or source-owner permission/quota resolution for the blocked files. After any h5ad is recovered, stage it to GCS, run the duplicate gate against public `main` plus `jkobject`, then smoke-first chunked ingestion; do not mark PRISM residuals done from metadata-only access probes.

## P6B-Mac post-PRISM huge dataset plan — 2026-06-21

Status artifact:

```text
artifacts/schema_audit/post_prism_huge_dataset_plan_20260621.md
artifacts/schema_audit/post_prism_huge_dataset_plan_20260621.json
artifacts/schema_audit/post_prism_phase3_manifest_20260621.json
```

Current decision:

- T-cell GWPS is owned by active P6A-Mac task `t_79528757`; do not run another huge ingestion in parallel.
- XAtlas/Orion is the next real huge missing candidate. Raw h5ads and sidecars are already staged on GCS, but no obs/X/var triplets exist yet (`xatlas/orion/manifest.parquet` only). The h5ads are ~195GiB HCT116 and ~326GiB HEK293T; this Mac has ~175GiB free, so no local backed-mode smoke should run here.
- Existing `tools/ingest_xatlas_orion.py` is a reference only for P6B: it downloads into `data/main`, uses global `ln.connect`, and materializes backed slices with `.to_memory()`. Patch/build a `connect_pertdata()` chunker and smoke on a large-disk/high-memory host before writing real triplets.
- RxRx/Recursion datasets are absent from Lamin but are image/Cell Painting data; do not force them into expression triplets before a representation contract (external image artifacts + `obs` + `obsm_<name>`/typed feature matrices, with JUMP overlap check).
- Sanger dual-guide CRC is no longer missing: `sanger_dual_guide_crc/mapping_counts/{obs.parquet,X.h5ad,var.parquet}` and `mapping_manifest.parquet` exist in Lamin and were reverified on 2026-06-22 (2,081,431 rows × 1 read-count feature; 13 samples; 2 walks; 8,911 guide pairs; raw `MAPPING.zip` staged on GCS).
- PRoPER-seq remains source-TBD; legacy GSE150818 stays excluded.

## P6A-Mac T-cell GWPS remote chunking — 2026-06-22

Current state: `D4_Rest.assigned_guide` is ingested and final-verified as chunked canonical triplets on `laminlabs/pertdata` branch `jkobject`. No duplicate T-cell GWPS writer is active.

What was validated:

- GCS staging is present for all 12 assigned-guide h5ads under `gs://scperturb/pert-gym/staging/data/main/tcell_gwps/raw/...`; total assigned-guide size is ~1.74 TB. Smallest file is `D4_Rest.assigned_guide.h5ad` at 118,609,904,492 bytes.
- Mac mini free space was only ~175–176 GiB at design time and ~171–173 GiB after completion, so full local materialization of even the smallest h5ad remains unsafe once outputs/cache are included.
- `tools/ingest_tcell_gwps_remote_chunks.py` implements the safe path: fsspec/gcsfs range reads + h5py CSR row slicing + canonical Lamin triplets, with no full h5ad copy into `data/gcs_cache/`.
- Dry probe for `D4_Rest.assigned_guide` read remote metadata and chunk 0 in bounded mode: 2,693,903 cells x 18,130 genes, 539 chunks at chunk_size=5000.
- Smoke write revised and verified `tcell_gwps/D4_Rest.assigned_guide/chunk_0000` as rows 0:5000; verification reported 5,000 obs rows, 18,130 var rows, X n_observations=5,000, canonical obs fields present (`perturbation`, `guide_id`, `is_control`, `organism`, `cell_type`, `modality`, `assay`), 144 controls.
- Original Hermes background runner verified chunks `0001..0003` and then died after `SAVE_CHUNK chunk_0004` without `VERIFY_CHUNK`; the retry found no duplicate writer, installed `tmux`, and resumed safely from `chunk_0004`.
- Resume runner `tcell-gwps-D4-Rest` completed chunks `0004..0538`; final chunk `0538` has 3,903 obs rows, 18,130 var rows, X n_observations=3,903, canonical obs fields present, and 112 controls.
- Final lightweight verification (`artifacts/schema_audit/tcell_gwps_D4_Rest_final_verification.json`) passed at 2026-06-22 09:45 CEST: 1,617 latest Lamin artifact records = 539 chunks x obs/X/var, no missing records, no bad row counts, no extra latest records, no bad log checks. It did not load any X payloads; it bulk-checked artifact records, used ingestion-log `VERIFY_CHUNK` checks for chunks `0001..0538`, and freshly read obs/var + obs→X→var links for chunks `0000`, `0269`, and `0538`.

Durable artifacts:

```text
tools/ingest_tcell_gwps_remote_chunks.py
artifacts/scripts/run_tcell_gwps_D4_Rest_chunked.sh
artifacts/scripts/run_tcell_gwps_D4_Rest_resume_from_0004.sh
artifacts/scripts/verify_tcell_gwps_D4_Rest_final.py
artifacts/run/tcell_gwps_D4_Rest_chunked.latest
artifacts/run/tcell_gwps_D4_Rest_lightweight_final_verify.latest
artifacts/run/tcell_gwps_D4_Rest_tmux_session.txt
artifacts/logs/tcell_gwps_D4_Rest_chunked_20260621_144525.log
artifacts/logs/tcell_gwps_D4_Rest_resume_from_0004_20260621_174300.log
artifacts/logs/tcell_gwps_D4_Rest_lightweight_final_verify_20260622_094511.log
artifacts/schema_audit/tcell_gwps_D4_Rest_final_verification.json
artifacts/tcell_gwps_D4_Rest_inflight_status.json
artifacts/tcell_gwps_remote_chunk_ingestion_status.json
```

Operational notes:

- Resume command if future T-cell GWPS files follow this pattern: `uv run python tools/ingest_tcell_gwps_remote_chunks.py --dataset <dataset> --chunk-size 5000 --start-chunk <N> --max-chunks <remaining>` from the repo, preferably through a timestamped tmux runner. The script skips existing triplets unless `--overwrite` is set.
- The script checkpoints `artifacts/tcell_gwps_remote_chunk_ingestion_status.json`; `artifacts/tcell_gwps_D4_Rest_inflight_status.json` now records the terminal verified state for this run.
- Non-blocking Lamin warnings observed: missing optional `botocore[crt]` credential-provider dependency and missing `ln.track()` provenance. Writes/verifications still succeeded on `laminlabs/pertdata` branch `jkobject`.
